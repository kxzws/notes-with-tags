import * as styled from './styled';

export const Header = () => {
  return (
    <styled.Header>
      <styled.HeaderContainer>
        <styled.Heading>notes-with-tags</styled.Heading>
        <p>Create new note</p>
        <p>Filter</p>
      </styled.HeaderContainer>
    </styled.Header>
  );
};
